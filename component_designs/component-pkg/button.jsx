function normalButton(text, onClickFunc) {
  return (
    <>
      <button onClick={onClickFunc} class="nor-button">{text}</button> 
    </>
  )
}

function normalButton(text, onClickFunc) {
  return (
    <>
      <button onClick={onClickFunc} class="pro-button">{text}</button> 
    </>
  )
}